[package]
name = "scs-obs"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OBS install probe and WebSocket client stubs (no session in M1)"

[dependencies]
serde.workspace = true
