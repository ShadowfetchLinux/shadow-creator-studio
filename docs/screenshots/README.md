# Screenshots

Add PNG captures of the Milestone 1 shell here:

- `record.png`
- `settings.png`
- `diagnostics.png`
- `wizard.png`

The README links these paths. They are placeholders until `scs-ui` is built
and launched on a machine with GTK4/libadwaita development packages.
